spring:
  profiles:
    active: dev
---
spring:
  config:
    activate:
      on-profile: dev
greeting:
  message: "Hello updated"
---
spring:
  config:
    activate:
      on-profile: prod
greeting:
  message: "Hello from prod"